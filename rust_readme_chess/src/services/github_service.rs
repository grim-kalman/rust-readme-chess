/// Communicates with the GitHub API to update the README file.
pub struct GithubService {
    // TODO: fields for HTTP client, auth token, etc.
}

impl GithubService {
    pub fn new(token: &str) -> Self {
        unimplemented!()
    }

    pub async fn update_readme(&self, board_markdown: &str) {
        unimplemented!()
    }
}
